# Copie para .env e preencha com valores reais antes de subir em produção.
# Nunca commitar o .env preenchido (já está no .gitignore).

POSTGRES_PASSWORD=troque-por-uma-senha-forte
DJANGO_SECRET_KEY=troque-por-uma-secret-key-gerada
DEBUG=False
